---
name: Bug report
about: Something didn't behave as expected
labels: bug
---

**What happened**
A clear description. If a rule failed to block (or wrongly blocked), say which.

**Reproduce**
1. Rule (`.anchor.toml` snippet):
2. Command / task given to the agent:
3. `anchor check` output (`anchor check --tool ... --command ...`):

**Expected vs actual**

**Environment**
- OS:
- Python (`python --version`):
- Anchor version / commit:
- `anchor doctor` output:
