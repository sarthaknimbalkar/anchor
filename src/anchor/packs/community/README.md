# Community rule-packs

Share your `.anchor.toml` rule-packs here. Open a PR adding `<name>.toml` to this
directory with a short description. Packs are plain Anchor rule files (see the main
README for the format). Like ESLint configs or dotfiles - copy what fits, ignore the rest.
